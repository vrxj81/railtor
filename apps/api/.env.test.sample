# Copy this file to .env.test.local and adjust as needed for automated tests.
# Values mirror docker-compose defaults.

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=api_development
DB_TEST_NAME=api_test
