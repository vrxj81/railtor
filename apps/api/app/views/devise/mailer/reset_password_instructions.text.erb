Hello <%= @resource.email %>,

We received a request to reset your Railtor password. You can continue the
process by visiting the link below:

<% web_app_url = Rails.application.config.x.web_app_url %>
<% reset_link = "#{web_app_url}/reset-password?token=#{@token}" %>
<%= reset_link %>

If you did not request a password reset, you can ignore this message.

— The Railtor Team
